basepath: .
shared:
  labels:
    - name: run
      value: application
  volumes:
    - name: code
      mount: /code/packages
      path: /Users/chris/Sites/docker-yarn-workspace-demo/packages

containers:
  ts:
    image: node:next
    env:
    - name: NODE_ENV
      value: "development"
    command:
    - yarn
    - debug:ts
