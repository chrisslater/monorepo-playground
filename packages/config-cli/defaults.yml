basepath: .
shared:
  labels:
    - name: run
      value: application
  volumes:
    - name: code
      mount: /code
      path: /Users/chris/Sites/docker-yarn-workspace-demo

containers:
  ts:
    image: node:next
    command:
    - yarn
    - debug:ts
    ports:
    - name: http
      port: 80
      containerPort: 3000
      protocol: TCP
